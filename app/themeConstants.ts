/** localStorage key for site light/dark preference (matches iOS zen semantic colors). */
export const ZENBEAD_THEME_STORAGE_KEY = "zenbead-theme";

/** Dispatched on `window` when theme changes so multiple `ThemeToggle` instances stay in sync. */
export const ZENBEAD_THEME_CHANGE_EVENT = "zenbead-theme-change";
